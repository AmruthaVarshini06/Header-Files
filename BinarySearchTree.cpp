#include "BinarySearchTree.h"
int main() {
    BinarySearchTree BST;  // Creates a binary search tree
    BST.Insert(10);  //Insert an element 10 into the tree
    BST.Insert(20);  //Insert an element 20 into the tree
    TreeNode *root;
    BinarySearchTree BST1;
    BST.ADD(20,root);
}
